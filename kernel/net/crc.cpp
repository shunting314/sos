#include <kernel/net/crc.h>
#include <string.h>
#include <stdlib.h>

uint32_t crc32(const char *hex) {
  assert(false && "crc32 for hex string not implemented yet");
}

uint32_t crc32(const uint8_t* data, int len) {
  assert(false && "crc32 for binary blob not implemented yet");
}
